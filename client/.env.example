# 后端（队友 Express）真实地址与鉴权 Key —— 只在 Nuxt server(BFF) 侧使用，不下发浏览器
BACKEND_BASE_URL=http://0.0.0.0:4000/api/v1
BACKEND_API_KEY=dev-key-change-me

# ★ Water Quality Pipeline：随机森林 GB 等级预测（FastAPI 端口 8080）
PIPELINE_BASE_URL=http://localhost:8080

# Demo Mode 默认开关（true=无硬件也能演示）
NUXT_PUBLIC_DEMO_MODE=true

# 高德地图 JS API Key（用于浏览器端逆地理编码：lat/lng → 省市区/街道）
NUXT_PUBLIC_AMAP_KEY=0d4cc945c411d0ce7a36899978c1fb22
